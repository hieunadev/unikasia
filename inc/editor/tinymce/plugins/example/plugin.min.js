tinymce.PluginManager.add('example', function(editor, url) {
  editor.addButton('example', {
    text: 'Div Tour',
    icon: false,
    onclick: function() {
     editor.insertContent('<div id="load_tour">Load List Tour</div>');
    }
  })
});